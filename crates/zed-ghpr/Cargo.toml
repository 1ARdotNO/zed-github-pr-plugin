[package]
name = "zed-ghpr"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

# Zed extensions compile to a WebAssembly component; this crate is standalone and
# intentionally outside the main workspace (different target + toolchain).
[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

[workspace]
